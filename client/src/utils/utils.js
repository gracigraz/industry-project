// const fs = require("fs");

// // function to save answers to a JSON file
// export const saveAnsToJson = (answers) => {
//   const jsonData = JSON.stringify(answers, null, 2);
//   fs.writeFileSync("answers.json", jsonData, "utf8");
// };
